include:
  - roles/coder.yaml
  - roles/reviewer.yaml

agents:
  coder:
    model: gpt-4
    vars:
      personality: pragmatic
  reviewer:
    model: gpt-4
    vars:
      personality: nitpicky
tasks:
  - agent: coder
    input: build feature
  - agent: reviewer
    input: review feature
