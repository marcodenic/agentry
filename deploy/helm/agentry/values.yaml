image:
  repository: agentry
  tag: latest
  pullPolicy: IfNotPresent

queue:
  address: nats://nats:4222

autoscaler:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  scaleFactor: 10
  pollInterval: 5s

storage:
  size: 1Gi
  accessMode: ReadWriteOnce
