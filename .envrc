# Agentry Development Environment with Go 1.25 Optimizations
# This file automatically enables Go 1.25 experimental features when you cd into this directory

# Enable Go 1.25 experimental features by default
export GOEXPERIMENT=jsonv2,greenteagc

# Optional: Add agentry to PATH after building
if [ -f "./agentry" ]; then
    export PATH="$(pwd):$PATH"
fi

echo "🚀 Agentry development environment loaded with Go 1.25 optimizations"
echo "   Features: JSON v2 + Green Tea GC enabled automatically"
